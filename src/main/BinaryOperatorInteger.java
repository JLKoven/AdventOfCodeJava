package main;

@FunctionalInterface
public interface BinaryOperatorInteger {
//	PriorIntegralify<T> applydddd(); this works too
	Integer apply(Integer x, Integer y);
    //if we are implementing TailCall with a lambda, that'll go here 
	
	//not here that's not what interfaces are for 

}
